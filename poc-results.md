# POC 测试结果总结

## 初步测试结果

- **已发送的初始邮件数量**：3
- **已发送的跟进邮件数量**：1
- **检测到的回复数量**：0

### 测试流程：
1. **读取联系人**：从 Google Sheet 获取联系人信息。
2. **发送首封邮件**：对于所有 `status` 未标记为 `sent` 的联系人，发送初始邮件。
3. **检测回复**：对每个联系人，检查是否有新的回复，决定是否需要发送跟进邮件。
4. **发送跟进邮件**：如果未收到回复，发送第一个跟进邮件。

### 遇到的问题：
- **错误**：邮件发送失败的联系人（如 `Missing email`）。
- **限制**：目前没有处理退信、退订请求或多账号轮换等高级功能。

### 测试数据：
- 发送的邮件包括来自 `Harvard University`，`Stanford University`，`Yale University` 和 `Columbia University` 的联系人。
- 跟进邮件已发送给所有未回复的联系人。